package org.example.eiscuno.model.observer;

/**
 * Interface for an observer that receives updates from an observable object.
 */
public interface Observer {

    /**
     * Updates the observer with changes from the observable object.
     */
    void update();
}

